# Base URL the frontend uses to call the API.
# In dev, leave as /api and let Vite proxy to the backend (see vite.config.js).
# In production, set to your backend's full URL, e.g. https://api.example.com/api
VITE_API_URL=/api
